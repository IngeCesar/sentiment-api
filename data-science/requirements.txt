# ==========================================
# 1. MODEL DEPENDENCIES (ML Core)
# ==========================================
# Using >= to satisfy training environment requirements
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0

# ==========================================
# 2. API DEPENDENCIES (Server)
# ==========================================
# Pinned versions for stability
fastapi==0.127.1
uvicorn==0.40.0
pydantic==2.12.5

# ==========================================
# 3. UTILS
# ==========================================
requests==2.32.5
regex==2025.11.3
